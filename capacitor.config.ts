import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.personal.dispatcherlite',
  appName: 'dispatcher-app',
  webDir: 'dist'
};

export default config;
